import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Buy WAEC PIN, NECO Token & JAMB PIN Online | PayBills",
    description: "Get your WAEC result checker pin, NECO token, and JAMB registration pins instantly. Fast and reliable educational services on PayBills.",
    keywords: ["waec result checker pin online", "buy neco token nigeria", "jamb pin purchase", "nabteb pin online", "educational scratch cards", "exam pins nigeria"],
};

export default function EducationLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
